routes:
  /:
    template: home
  /services/:
    template: services
  /courses/:
    template: courses

collections:
  /blog/:
    permalink: /posts/{slug}/
    template: index

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
